# Hoist all Prisma packages to root node_modules so the generated client
# can resolve @prisma/client-runtime-utils and other internal packages.
public-hoist-pattern[]=@prisma/*
public-hoist-pattern[]=prisma
