✅ COMPLETE DEPLOYMENT CHECKLIST - CRON JOB + BUILD SETTINGS

═══════════════════════════════════════════════════════════════════════════

PART 1: VERCEL PROJECT SETTINGS (1 minute)
═════════════════════════════════════════════════════════════════════════════

☐ 1. Go to Vercel Build Settings
     https://vercel.com/projects/gsu-alumni-connect/settings/build-and-deployment

☐ 2. Update Build Command
     Current: npm run build OR next build (default)
     Change to: pnpm run vercel-build
     Click: Save

✅ Why: Ensures environment validation during build


PART 2: ENVIRONMENT VARIABLES (2 minutes)
═════════════════════════════════════════════════════════════════════════════

Go to: https://vercel.com/projects/gsu-alumni-connect/settings/environment-variables

Verify these exist (they should already be set):
  ☐ DATABASE_URL (Neon pooled connection)
  ☐ DIRECT_URL (Neon direct connection)
  ☐ BETTER_AUTH_SECRET
  ☐ IMPORT_QUEUE_REDIS_URL
  ☐ NEXT_PUBLIC_APP_URL (https://gsu-alumni-connect.vercel.app)

Add this NEW one:
  ☐ CRON_SECRET
     Value: [generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"]

✅ Why: Cron endpoint needs these to authenticate and process jobs


PART 3: DEPLOY CODE (3 minutes)
═════════════════════════════════════════════════════════════════════════════

Run in PowerShell:

cd c:\Users\user\Documents\gsu-alumni-connect

☐ 1. Check status
     git status

☐ 2. Stage all changes
     git add .

☐ 3. Commit with message
     git commit -m "feat(cron): add automatic job processing on Vercel
     
     - Add cron endpoint: /api/cron/process-import
     - Add cron config: vercel.json (every minute)
     - Add self-trigger: auto-call cron after job creation
     - Update Vercel build settings: use pnpm run vercel-build
     - Add CRON_SECRET environment variable"

☐ 4. Push to GitHub
     git push origin main

     Wait for: "Everything up-to-date" OR see deployment start


PART 4: MONITOR BUILD (5 minutes)
═════════════════════════════════════════════════════════════════════════════

☐ 1. Go to Deployments
     https://vercel.com/projects/gsu-alumni-connect/deployments

☐ 2. Watch for new deployment
     Should start within 30 seconds of your git push

☐ 3. Build should take:
     ~3-4 minutes total

☐ 4. You should see:
     ✅ Installing dependencies
     ✅ Running vercel-build (check-env, prisma, next build)
     ✅ Uploading bundle
     ✅ Green checkmark (success)

☐ 5. If build fails:
     - Click on deployment
     - Scroll to "Logs"
     - Read error message
     - Common issues:
       • Missing DATABASE_URL
       • Missing CRON_SECRET
       • Wrong environment variable value


PART 5: VERIFY CRON REGISTRATION (2 minutes)
═════════════════════════════════════════════════════════════════════════════

☐ 1. Go to Cron Jobs
     https://vercel.com/projects/gsu-alumni-connect/settings/cron-jobs

☐ 2. You should see:
     ✅ /api/cron/process-import
     ✅ Schedule: Every 1 minute

☐ 3. If you see this:
     ✅ Cron job is registered and WILL run every minute automatically!
     ✅ Skip to "Test" below

☐ 4. If you don't see it:
     - Wait 2 minutes and refresh
     - Then try: git push origin main again


PART 6: TEST THE SETUP (15 minutes)
═════════════════════════════════════════════════════════════════════════════

☐ 1. Open your app
     https://gsu-alumni-connect.vercel.app/admin/uploads

☐ 2. Click "Upload"

☐ 3. Select an Excel file

☐ 4. Click "Confirm"

☐ 5. Watch the status
     Should show: "Processing..." within 1 second
     
     Good signs:
     ✅ "Processing..." appears immediately
     ✅ No "timeout" or "error"
     ✅ Status eventually changes
     ✅ Shows "Completed" or "Almost done..."

     Bad signs (troubleshoot):
     ❌ Shows "Queued..." (stuck - check logs)
     ❌ "Timeout" error (job taking too long)
     ❌ Error message (check /api/cron/process-import logs)

☐ 6. Check function logs
     vercel logs --tail
     
     Look for:
     [cron/process-import] processing job...
     [import-worker] Pre-warming groups...
     [import-worker] Checkpoint: 100/...
     [cron/process-import] completed job...

☐ 7. File should complete in 5-15 minutes
     Then show "Completed" in UI


═════════════════════════════════════════════════════════════════════════════

SUMMARY OF CHANGES
═════════════════════════════════════════════════════════════════════════════

Created:
  ✅ app/api/cron/process-import/route.ts (cron endpoint)
  ✅ vercel.json (cron configuration)

Modified:
  ✅ app/api/import-jobs/route.ts (self-trigger added)
  ✅ package.json (cron:test script added)

Vercel Settings:
  ✅ Build Command: pnpm run vercel-build
  ✅ Environment Variable: CRON_SECRET (add)

Result:
  ✅ Automatic job processing on Vercel every minute
  ✅ Instant processing via self-trigger
  ✅ Zero manual intervention needed


═════════════════════════════════════════════════════════════════════════════

CHECKLIST FOR COMPLETION
═════════════════════════════════════════════════════════════════════════════

Before Deployment:
  ☐ All code saved and no unsaved files
  ☐ Vercel build command updated
  ☐ CRON_SECRET generated

Deployment:
  ☐ git push successful
  ☐ New deployment started in Vercel
  ☐ Build completed without errors

Verification:
  ☐ Cron job visible in Vercel dashboard
  ☐ File upload on /admin/uploads works
  ☐ Processing starts immediately
  ☐ Function logs show success

✅ If all checked: You're done! Automatic processing is live!


═════════════════════════════════════════════════════════════════════════════

TROUBLESHOOTING QUICK FIX
═════════════════════════════════════════════════════════════════════════════

Problem: Build fails with "DATABASE_URL not set"
Fix:     Add DATABASE_URL to Vercel environment variables

Problem: Cron doesn't show in dashboard
Fix:     Wait 2 minutes, refresh, or redeploy (git push origin main)

Problem: Upload stays "Processing..." forever
Fix:     Check function logs (vercel logs --tail)
         Verify CRON_SECRET is set
         Check /api/cron/process-import is accessible

Problem: "Timeout" error
Fix:     File might be too large
         Or database connection issue
         Check logs for specific error


═════════════════════════════════════════════════════════════════════════════

NEXT: Follow this checklist top to bottom

Estimated time: 30 minutes total
Result: Automatic job processing on Vercel forever! 🎉

═════════════════════════════════════════════════════════════════════════════
